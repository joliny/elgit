define([
    'jquery',
    'underscore',
    'backbone'
], function($, _, Backbone) {
    var viewRepoTags = Backbone.View.extend({
        render: function() {
            // nothing to do here...
        }
    });

    return new viewRepoTags;
});